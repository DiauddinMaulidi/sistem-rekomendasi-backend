step for runing this project:

1. pip install uvicorn
2. python -m pip install xgboost==3.3.0
3. python -m pip install scikit-learn==1.8.0
4. python -m pip install joblib pandas supabase
5. python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
